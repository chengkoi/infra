server:
  port: 8080
  mode: debug
  read_timeout: 60
  write_timeout: 60

database:
  driver: postgres
  host: localhost
  port: 5432
  username: postgres
  password: "tiger"
  database: infra
  max_idle_conns: 10
  max_open_conns: 100
  conn_max_lifetime: 3600

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  pool_size: 10

jwt:
  secret: infra-jwt-secret-key-dev
  expire: 7200

log:
  level: debug
  filename: logs/app.log
  max_size: 100
  max_backups: 3
  max_age: 7
  compress: true

captcha:
  enabled: false
  mode: digit             # digit | string | math | chinese
  length: 4               # 验证码长度
  width: 240              # 图片宽度
  height: 80              # 图片高度
  expire: 300             # 过期时间(秒)


oss:
  endpoint: oss-cn-hangzhou.aliyuncs.com
  access_key_id: ""
  access_key_secret: ""
  bucket: ""
  base_path: uploads/
